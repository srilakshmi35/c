#include<stdio.h>
void main()
{
	char str[500],str1[50];
	char *ptr,*ptr1;
	int length=0,len=0;
	scanf("%s",str);
	scanf("%s",str1);
	for(ptr=&str;*ptr!='\0';ptr++)
	{
		lenght++;
	}
	for(ptr=&str1;*ptr!='\0';ptr++)
	{
		len;
	}
	
}
